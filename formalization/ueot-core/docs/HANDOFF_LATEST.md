# UEOT Core 3 Lean — Fallback Handoff Snapshot

> GitHub Issue #56 is the live cross-chat construction state when available.
> This file is the fallback archival snapshot and is updated at meaningful
> lifecycle transitions.

## Current lifecycle snapshot

- source P-IDs: **106**;
- authoritative full-green baseline before this ledger checkpoint: **77/106**;
- full-green 77 baseline: `main@6561169fae9529a822642c5b6921d7d9c59aa250`;
- P-PER-04 proof main: `ba9b9c350038dafa52afdda954d2294361a01fc7`;
- P-PER-04 proof resulting-main CI: `34863965181` — success;
- ledger branch: `formal/ledger-78-pper04`;
- this branch stages **78/106**;
- remaining not-yet-counted P-IDs after staging: **28**;
- active uncounted proof lanes after this staging: **0**.

**Do not call 78/106 full-green until this ledger/recovery branch passes its own
branch CI, PR CI, lands on `main`, and the resulting main CI succeeds.**

## P-PER-04 — proof complete, ledger promotion running

Frozen Core 3 §8.6 is represented at its declared tangency-necessity scope:

1. the viability/identity domain is closed;
2. the viable trajectory is differentiable at time zero and remains in the domain for nonnegative time;
3. the source uses the positive-time Bouligand contingent cone (`h ↓ 0`);
4. the conclusion is only that the initial velocity lies in that cone;
5. Mathlib `posTangentConeAt = tangentConeAt NNReal` supplies the matching positive-scalar object;
6. positive-time difference quotients are generated by `HasDerivAt.tendsto_slope_zero_right`;
7. K-VIA-01 (the separate Marchaud/Nagumo viability theorem) is not folded into P-PER-04.

Canonical theorem:
- `UEOT.V3.ViabilityTangency.p_per_04`.

Evidence:
- feature `formal/pper04-tangency-necessity-v1@ce6eccf5ca4b6f30aae1dcd0416fc1b79286f2d2`;
- feature CI `34858878140` success;
- source semantic audit complete;
- pinned-Mathlib tangent-cone definition audit complete;
- prohibited-proof audit clean;
- clean integration `formal/pper04-main-integration-v1@f5db4c5e547866e488ad1d33c0789bc0ce5ec48c`;
- clean integration CI `34862446244` success;
- PR #83 PR CI `34863236502` success;
- proof main `ba9b9c350038dafa52afdda954d2294361a01fc7`;
- proof resulting-main CI `34863965181` success.

Exact next action: complete the separate **78/106** ledger/recovery branch → PR →
main resulting-CI lifecycle.

## Previous full-green promotion — P-QSD-03

P-QSD-03 is already counted in the authoritative 77/106 baseline at
`main@6561169fae9529a822642c5b6921d7d9c59aa250`. Its ledger resulting-main run
`34856357799` succeeded on attempt 3; attempts 1 and 2 were external HTTP 504
failures before UEOT compilation and did not constitute repository regressions.

## Branchless next-front audit

No second proof branch is open during the P-PER-04 ledger lifecycle.

- **P-PER-02:** non-quick; requires Polish/Feller, tight time-averaged laws,
  Prokhorov subsequences, Feller invariance and Portmanteau support preservation.
- **P-REC-03/04:** plausible medium next lanes; pinned Mathlib has discrete
  hitting-time and optional-stopping machinery, but the source still requires a
  genuine Markov first-step identity / stopped-drift construction.
- **P-COMP-01:** main already contains genuine binary conditional-mutual-info and
  KL infrastructure, but the frozen arbitrary finite-partition, multi-block
  product-law equivalence remains missing; a binary wrapper is not countable.

## Guards

- do not reopen counted green P-IDs absent source mismatch/CI regression;
- feature green or proof-main green never increments coverage;
- no `sorry`, `admit`, `native_decide`, or unsourced `axiom`;
- preserve frozen source strength; do not replace hard clauses by convenient finite/toy/Markov-only surrogates;
- P-QSD-01 and P-QSD-03 must never be swapped;
- P-DDH-04/05 require genuine rank/singular-value infrastructure;
- P-KL-04/05 must remain at their frozen CTMC/Girsanov level.

## Recovery order

1. `NEW_CHAT_BOOTSTRAP.md`;
2. `docs/REPOSITORY_BRANCH_GOVERNANCE.md`;
3. `UEOT_CORE3_LEAN_OPERATIONS.md`;
4. Issue #56 when available;
5. `PID_STATUS.yaml`;
6. `FORMALIZATION_STATE.md`;
7. `V3_COVERAGE_STATUS.md`;
8. this `HANDOFF_LATEST.md` fallback snapshot;
9. live main/branches/CI reconciliation.
